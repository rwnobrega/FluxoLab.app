Variáveis
    a, b, c: número
    s: número
    area: número
Início
    Escreva "Entre com os três lados do triângulo:"
    Leia a, b, c
    s = (a + b + c) / 2
    area = sqrt(s * (s - a) * (s - b) * (s - c))
    Escreva "Área: ", area
Fim
